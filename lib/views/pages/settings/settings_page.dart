import 'package:flutter/material.dart';

class SettinngsPage extends StatelessWidget {
  const SettinngsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
      title: Text("SettinngsPage"),
    ));
  }
}
